system:
  enabled: true
  authLogs:
    vars:
      paths:
        - /var/log/custom_auth.log
  syslog:
    vars:
      paths:
        - /var/log/custom_syslog.log
kubernetes:
  enabled: false
agent:
  unprivileged: true
