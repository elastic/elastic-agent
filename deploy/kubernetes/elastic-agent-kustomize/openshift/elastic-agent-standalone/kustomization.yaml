apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - ../../default/elastic-agent-standalone
  - elastic-agent-standalone-scc-role-binding.yaml
  - kube-state-metrics-scc-role-binding.yaml
