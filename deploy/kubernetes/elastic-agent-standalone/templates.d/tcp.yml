inputs:
    - name: tcp-tcp
      type: tcp
      use_output: default
      streams:
        - condition: ${kubernetes.hints.tcp.generic.enabled} == true or ${kubernetes.hints.tcp.enabled} == true
          data_stream:
            dataset: tcp.generic
            type: logs
          host: localhost:8080
          processors: ${kubernetes.hints.tcp.generic.processors}
      data_stream.namespace: default
    - name: filestream-tcp
      type: filestream
      use_output: default
      streams:
        - condition: ${kubernetes.hints.container_logs.enabled} == true
          data_stream:
            dataset: kubernetes.container_logs
            type: logs
          exclude_files: []
          exclude_lines: []
          parsers:
            - container:
                format: auto
                stream: all
          paths:
            - /var/log/containers/*${kubernetes.hints.container_id}.log
          processors: ""
          prospector:
            scanner:
                symlinks: true
          tags: []
      data_stream.namespace: default
