version: 2
inputs:
  - name: pf-elastic-collector
    description: "Universal Profiling Collector"
    platforms:
      - linux/amd64
      - linux/arm64
      - container/amd64
      - container/arm64
    outputs:
      - elasticsearch
    command:
      args:
        - "-elastic"
