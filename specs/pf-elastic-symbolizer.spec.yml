version: 2
inputs:
  - name: pf-elastic-symbolizer
    description: "Universal Profiling Symbolizer"
    platforms:
      - linux/amd64
      - linux/arm64
      - container/amd64
      - container/arm64
    outputs:
      - elasticsearch
    command:
      args:
        - "-elastic"
