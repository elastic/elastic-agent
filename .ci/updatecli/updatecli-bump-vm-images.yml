# update-cli configuration for automated VM image version bumping
---
name: Bump vm-images to latest version
pipelineid: 'updatecli-update-vm-images-{{ requiredEnv "BRANCH_NAME" }}'

scms:
  githubConfig:
    kind: github
    spec:
      user: '{{ requiredEnv "GITHUB_ACTOR" }}'
      username: '{{ requiredEnv "GITHUB_ACTOR" }}'
      owner: '{{ .scm.owner }}'
      repository: '{{ .scm.repository }}'
      token: '{{ requiredEnv "GITHUB_TOKEN" }}'
      commitusingapi: true
      branch: '{{ requiredEnv "BRANCH_NAME" }}'
      force: false
      submodules: false

actions:
  elastic-agent:
    kind: github/pullrequest
    scmid: githubConfig
    sourceid: latestGoVersion
    spec:
      automerge: false
      labels:
        - dependencies
        - backport-skip
        - skip-changelog
      title: '[{{ requiredEnv "BRANCH_NAME" }}][Automation] Bump VM Image version to {{ source "latestVersion" }}'

sources:
  latestVersion:
    name: Get latest available build
    kind: json
    spec:
      file: https://storage.googleapis.com/artifacts-api/vm-images/elastic-agent/latest.json
      key: .date

conditions:
  latestVersion-check:
    name: Check if defined latest version differs
    kind: shell
    sourceid: latestVersion
    spec:
      command: 'grep -q -v {{ source "latestVersion" }} .buildkite/pipeline.yml #'

# NOTE: if you add a new target file, please update the .mergify.yml file
#       to include the new file for the approval and automatic merge
targets:
  update-buildkite-pipelines:
    name: "Update all Buildkite pipeline files (GCP/AWS)"
    sourceid: latestVersion
    scmid: githubConfig
    kind: file
    spec:
      files:
        - .buildkite/pipeline.yml
        - .buildkite/bk.integration.pipeline.yml
        - .buildkite/bk.integration-fips.pipeline.yml
        - .buildkite/integration.pipeline.yml
        - .buildkite/pipeline.elastic-agent-binary-dra.yml
        - .buildkite/pipeline.elastic-agent-package.yml
      matchpattern: '(IMAGE_.+): "platform-ingest-elastic-agent-(.+)-([0-9]+)"'
      replacepattern: '$1: "platform-ingest-elastic-agent-$2-{{ source "latestVersion" }}"'

  update-azure-buildkite-pipelines:
    name: "Update all Buildkite pipeline files (Azure)"
    sourceid: latestVersion
    scmid: githubConfig
    kind: file
    spec:
      files:
        - .buildkite/pipeline.yml
        - .buildkite/bk.integration.pipeline.yml
        - .buildkite/bk.integration-fips.pipeline.yml
      matchpattern: '(IMAGE_.+): "1.0.([\d]+)"'
      replacepattern: '$1: "1.0.{{ source "latestVersion" }}"'
