kind: feature
summary: Agent can be built for `darwin/arm64`. When it's built for both `darwin/arm64` and `darwin/adm64` a universal binary is also built and packaged.
pr: https://github.com/elastic/elastic-agent/pull/203
