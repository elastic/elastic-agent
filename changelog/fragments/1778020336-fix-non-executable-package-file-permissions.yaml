kind: bug-fix

summary: Ship config samples and endpoint resources zip without executable bits

description: |
  Several non-executable files were being installed with mode 0755 in
  Elastic Agent packages These files now ship with mode 0644 across deb, rpm,
  tar.gz, and zip packages.

component: elastic-agent
pr: https://github.com/elastic/elastic-agent/pull/14117
