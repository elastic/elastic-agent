# yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
# Diagnostic pipeline: two Windows 11 variants for golang/go#77975.
# "diagnostic" run: GOGC=1 forces a GC after every alloc to maximise the
# shrinkstack/scanstack frequency and make the IOCP stack-corruption crash
# more consistent. "instrumented" run: WER dumps + clobberfree + gctrace.

# This section is used to define the plugins that will be used in the pipeline.
# See https://buildkite.com/docs/pipelines/integrations/plugins/using#using-yaml-anchors-with-plugins
common:
  - google_oidc_plugin: &google_oidc_plugin
      # See https://github.com/elastic/oblt-infra/blob/main/conf/resources/repos/elastic-agent/01-gcp-oidc.tf
      # This plugin authenticates to Google Cloud using the OIDC token.
      # Required for buildkite-agent artifact upload to GCS, including the
      # WER crash dumps the instrumented job depends on.
      elastic/oblt-google-auth#v1.3.1:
        lifetime: 10800 # seconds
        project-id: "elastic-observability-ci"
        project-number: "911195782929"
  - sparse_checkout_plugin: &sparse_checkout_plugin
      sparse-checkout#v1.6.0:
        paths:
        - .buildkite
        - .go-version
        - .package-version
        - version/version.go

env:
  IMAGE_WIN_11: "platform-ingest-elastic-agent-windows-11-pro"
  IMAGE_WIN_11_VERSION: "1.0.1779464011"

steps:
  - label: "Env probe - Windows 11 (run %n)"
    key: "envprobe-win11"
    # Characterise the CI execution context (std-handle types incl. ConPTY,
    # job object, affinity/priority, scheduling jitter / vCPU-steal sample,
    # process tree, loaded modules / injected DLLs, Defender, hypervisor) so we
    # can diff it against a manually-provisioned VM (same image/HW) that does
    # NOT reproduce, and find the confound. Deterministic - no crash needed.
    # parallelism 3 to sample a few different CI hosts.
    command: .buildkite/scripts/steps/envprobe.ps1
    parallelism: 3
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 (narrow TestRollback run %n)"
    key: "unit-tests-win11-rollback"
    # Test-case narrowing: upgrade package scoped to -run=TestRollback (covers
    # TestRollback + TestRollbackWithOpts). If this crashes, that family alone is
    # sufficient to trigger the bug. Compared against the full-upgrade control
    # and the TestCleanup narrowing below.
    command: .buildkite/scripts/steps/unit-tests-instrumented.ps1
    parallelism: 10
    env:
      DIAG_CAPTURE: "1"
      TEST_PKG: "./internal/pkg/agent/application/upgrade/"
      TEST_RUN: "TestRollback"
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
      - "build/dumps/*.dmp"
      - "build/bins/*.exe"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 (diag-capture upgrade-scoped run %n)"
    key: "unit-tests-win11-diagcapture"
    # Dump-capturing variant, SCOPED to the upgrade package (TEST_PKG) so every
    # crash lands in a single rebuildable binary (upgrade.test.exe). The
    # whole-suite diag-capture (build 40693) crashed 5/5 but 4 of those landed in
    # the mage-target "test.test.exe", which can't be rebuilt - leaving those
    # dumps (possibly the "unexpected signal" AVs we want) unsymbolicable. Only
    # the one crash that happened to land in upgrade was usable. Scoping fixes
    # that: whatever signature fires - checkmark, sweep, or the prized
    # "unexpected signal" (GC stack-scan AV, FailFast carrying the real
    # 0xC0000005 + fault PC + addr) - it comes with a byte-matching binary.
    # DIAG_CAPTURE=1 = diagnostic GODEBUG (GOGC=1 + gccheckmark + invalidptr).
    # parallelism 8 for more shots; ~40% of crashes are the AV variant.
    # (If control crashes but this does not, upgrade-alone is insufficient load
    # and we should broaden the scope.)
    command: .buildkite/scripts/steps/unit-tests-instrumented.ps1
    parallelism: 8
    env:
      DIAG_CAPTURE: "1"
      TEST_PKG: "./internal/pkg/agent/application/upgrade/"
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
      - "build/dumps/*.dmp"
      - "build/bins/*.exe"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 (narrow TestCleanup run %n)"
    key: "unit-tests-win11-cleanup"
    # Test-case narrowing: upgrade package scoped to -run=TestCleanup. Companion
    # to the TestRollback narrowing - together they tell us whether one family
    # alone triggers the bug, or whether it needs the combined workload (only
    # the full-upgrade control crashing would imply the latter).
    command: .buildkite/scripts/steps/unit-tests-instrumented.ps1
    parallelism: 10
    env:
      DIAG_CAPTURE: "1"
      TEST_PKG: "./internal/pkg/agent/application/upgrade/"
      TEST_RUN: "TestCleanup"
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
      - "build/dumps/*.dmp"
      - "build/bins/*.exe"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: ":junit: Junit annotate"
    agents:
      image: "docker.elastic.co/ci-agent-images/buildkite-junit-annotate:1.0"
    plugins:
      - junit-annotate#v2.7.0:
          artifacts: "**TEST-*.xml"
          always-annotate: true
          run-in-docker: false
      - *google_oidc_plugin
    depends_on:
      - step: "unit-tests-win11-diagcapture"
        allow_failure: true
      - step: "unit-tests-win11-rollback"
        allow_failure: true
      - step: "unit-tests-win11-cleanup"
        allow_failure: true

  - wait: ~
