# yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
# Diagnostic pipeline: two Windows 11 variants for golang/go#77975.
# "diagnostic" run: GOGC=1 forces a GC after every alloc to maximise the
# shrinkstack/scanstack frequency and make the IOCP stack-corruption crash
# more consistent. "instrumented" run: WER dumps + clobberfree + gctrace.

# This section is used to define the plugins that will be used in the pipeline.
# See https://buildkite.com/docs/pipelines/integrations/plugins/using#using-yaml-anchors-with-plugins
common:
  - google_oidc_plugin: &google_oidc_plugin
      # See https://github.com/elastic/oblt-infra/blob/main/conf/resources/repos/elastic-agent/01-gcp-oidc.tf
      # This plugin authenticates to Google Cloud using the OIDC token.
      # Required for buildkite-agent artifact upload to GCS, including the
      # WER crash dumps the instrumented job depends on.
      elastic/oblt-google-auth#v1.3.1:
        lifetime: 10800 # seconds
        project-id: "elastic-observability-ci"
        project-number: "911195782929"
  - sparse_checkout_plugin: &sparse_checkout_plugin
      sparse-checkout#v1.6.0:
        paths:
        - .buildkite
        - .go-version
        - .package-version
        - version/version.go

env:
  IMAGE_WIN_11: "platform-ingest-elastic-agent-windows-11-pro"
  IMAGE_WIN_11_VERSION: "1.0.1779464011"

steps:
  - label: "Unit tests - Windows 11 (diagnostic run %n)"
    key: "unit-tests-win11"
    command: .buildkite/scripts/steps/unit-tests.ps1
    parallelism: 10
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 (norace run %n)"
    key: "unit-tests-win11-norace"
    # Same diagnostic config as unit-tests-win11, but with the race detector
    # disabled on amd64 (NO_RACE=1). The race detector is only supported on
    # windows/amd64 - never arm64 - so the arm64 runs that "don't crash" never
    # had -race at all, confounding the architecture comparison. This variant
    # de-confounds it on the same Win11 amd64 VM: if amd64 still crashes without
    # -race, the bug is independent of the race detector and the arm64/Win10
    # differences are a real environment signal; if it stops crashing, the
    # repro depends on the race detector's instrumentation/scheduling timing.
    #
    # (Green Tea GC was ruled out: the crash reproduces with GOEXPERIMENT=
    # nogreenteagc and on Go 1.25.10, so the nogreenteagc variant was retired.
    # The earlier gcshrinkstackoff=1 variant was also retired: build 40538
    # showed it failed at the same ~7/10 rate as the plain variant.)
    command: .buildkite/scripts/steps/unit-tests.ps1
    parallelism: 10
    env:
      NO_RACE: "1"
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 (instrumented run %n)"
    key: "unit-tests-win11-instrumented"
    command: .buildkite/scripts/steps/unit-tests-instrumented.ps1
    parallelism: 5
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
      - "build/dumps/*.dmp"
      - "build/bins/*.exe"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: ":junit: Junit annotate"
    agents:
      image: "docker.elastic.co/ci-agent-images/buildkite-junit-annotate:1.0"
    plugins:
      - junit-annotate#v2.7.0:
          artifacts: "**TEST-*.xml"
          always-annotate: true
          run-in-docker: false
      - *google_oidc_plugin
    depends_on:
      - step: "unit-tests-win11"
        allow_failure: true
      - step: "unit-tests-win11-norace"
        allow_failure: true
      - step: "unit-tests-win11-instrumented"
        allow_failure: true

  - wait: ~
