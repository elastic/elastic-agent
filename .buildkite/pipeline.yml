# yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
# Diagnostic pipeline: two Windows 11 variants for golang/go#77975.
# "diagnostic" run: GOGC=1 forces a GC after every alloc to maximise the
# shrinkstack/scanstack frequency and make the IOCP stack-corruption crash
# more consistent. "instrumented" run: WER dumps + clobberfree + gctrace.

# This section is used to define the plugins that will be used in the pipeline.
# See https://buildkite.com/docs/pipelines/integrations/plugins/using#using-yaml-anchors-with-plugins
common:
  - google_oidc_plugin: &google_oidc_plugin
      # See https://github.com/elastic/oblt-infra/blob/main/conf/resources/repos/elastic-agent/01-gcp-oidc.tf
      # This plugin authenticates to Google Cloud using the OIDC token.
      # Required for buildkite-agent artifact upload to GCS, including the
      # WER crash dumps the instrumented job depends on.
      elastic/oblt-google-auth#v1.3.1:
        lifetime: 10800 # seconds
        project-id: "elastic-observability-ci"
        project-number: "911195782929"
  - sparse_checkout_plugin: &sparse_checkout_plugin
      sparse-checkout#v1.6.0:
        paths:
        - .buildkite
        - .go-version
        - .package-version
        - version/version.go

env:
  IMAGE_WIN_11: "platform-ingest-elastic-agent-windows-11-pro"
  IMAGE_WIN_11_VERSION: "1.0.1779464011"
  IMAGE_WIN_10: "platform-ingest-elastic-agent-windows-10-pro"
  IMAGE_WIN_10_VERSION: "1.0.1779464011"
  IMAGE_WIN_11_ARM64: "platform-ingest-elastic-agent-windows-11-pro-arm64"
  IMAGE_WIN_11_ARM64_VERSION: "1.0.1779464011"

steps:
  - label: "Unit tests - Windows 11 amd64 (diagnostic run %n)"
    key: "unit-tests-win11"
    command: .buildkite/scripts/steps/unit-tests.ps1
    parallelism: 10
    artifact_paths:
      - "build/TEST-*.html"
      - "build/TEST-*.xml"
      - "build/diagnostics/*"
      - "coverage-*.out"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11}"
      imageVersion: "${IMAGE_WIN_11_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  # OS/arch matrix to test the "Windows-11 + amd64 specific" hypothesis.
  # All three use the SAME diagnostic script and Azure provider, so the only
  # variables are Windows version (11 vs 10) and CPU arch (amd64 vs arm64).
  #   - win10-amd64 clean + win11-amd64 crashes -> Windows 11-specific.
  #   - win11-arm64 clean + win11-amd64 crashes -> amd64-specific.
  #   - both clean -> specifically Windows 11 + amd64 (the hypothesis).
  - label: "Unit tests - Windows 10 amd64 (diagnostic run %n)"
    key: "unit-tests-win10-amd64"
    command: .buildkite/scripts/steps/unit-tests.ps1
    parallelism: 10
    artifact_paths:
      - "build/TEST-*.xml"
      - "coverage-*.out"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_10}"
      imageVersion: "${IMAGE_WIN_10_VERSION}"
      vmSize: "Standard_D8s_v5"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: "Unit tests - Windows 11 arm64 (diagnostic run %n)"
    key: "unit-tests-win11-arm64"
    command: .buildkite/scripts/steps/unit-tests.ps1
    parallelism: 10
    artifact_paths:
      - "build/TEST-*.xml"
      - "coverage-*.out"
    agents:
      provider: "azure"
      image: "${IMAGE_WIN_11_ARM64}"
      imageVersion: "${IMAGE_WIN_11_ARM64_VERSION}"
      vmSize: "Standard_D8ps_v6"
      diskSizeGb: "256"
    retry:
      manual:
        allowed: true
    plugins:
      - *google_oidc_plugin

  - label: ":junit: Junit annotate"
    agents:
      image: "docker.elastic.co/ci-agent-images/buildkite-junit-annotate:1.0"
    plugins:
      - junit-annotate#v2.7.0:
          artifacts: "**TEST-*.xml"
          always-annotate: true
          run-in-docker: false
      - *google_oidc_plugin
    depends_on:
      - step: "unit-tests-win11"
        allow_failure: true
      - step: "unit-tests-win10-amd64"
        allow_failure: true
      - step: "unit-tests-win11-arm64"
        allow_failure: true

  - wait: ~
