namespace: kube-system
commonLabels:
  k8s-app: elastic-agent
namePrefix: elastic-agent-
resources:
  - ../../base/common
  - daemonset.yaml

configMapGenerator:
  - name: configs
    envs:
    - .env
