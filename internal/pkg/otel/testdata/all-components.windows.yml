receivers:
  iis:

exporters:
  file:
    path: ${env:TMP}\file-exporter-output.json
  nop:

service:
  pipelines:
    metrics/windows-only:
      exporters:
        - nop
      receivers:
        - iis
