type: elastic_diagnostics
github_project: elastic/elastic-agent

status:
  class: extension
  stability:
    development: [extension]
  codeowners:
    active: [VihasMakwana]

tests:
  config:
    endpoint: "localhost:6060"
